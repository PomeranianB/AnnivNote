<main>

<h3>全ユーザー一覧</h3>
<ul>
  <% @users.each do |user| %>
    <li>
    <%=link_to(admin_user_path(user.id), class: "text-dark") do %>
      <%= user.name %>
    <% end %>
     - <%= user.email %>
      - <%= link_to '退会させる', admin_user_path(user), method: :delete, data: { confirm: '本当に退会させますか？' }, class: "my-custum-link" %> 
    </li>
  <% end %>
</ul>

</main>